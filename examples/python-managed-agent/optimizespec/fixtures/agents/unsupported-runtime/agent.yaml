id: unsupported-runtime
name: unsupported-runtime
fixture_type: negative
runtime: Codex
description: Negative fixture for an unsupported non-Claude runtime.
source_files:
  agent: agents/codex_agent.py
existing_commands:
  direct_eval: codex eval
candidate_fields:
  - instructions
expected_failure: unsupported runtime
runtime_facts:
  - This fixture intentionally uses a non-Claude runtime.
v1_constraints:
  - Eval workflow validation supports Claude Managed Agents only.
expected_behavior:
  semantic_checks: {}
  live:
    requires_live: false
    required_env: []

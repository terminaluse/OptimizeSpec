id: missing-scoring-guidance
name: missing-scoring-guidance
fixture_type: negative
runtime: Claude Managed Agents
description: Negative fixture where examples exist but numeric and qualitative scoring guidance is missing.
source_files:
  runtime: src/optimizespec/runtime.py
existing_commands:
  direct_eval: optimizespec eval-demo
candidate_fields:
  - system_prompt
expected_failure: missing scoring guidance
runtime_facts:
  - Eval examples exist but scoring is ambiguous.
v1_constraints:
  - The workflow must ask for scoring clarification before optimization.
expected_behavior:
  semantic_checks: {}
  live:
    requires_live: false
    required_env:
      - ANTHROPIC_API_KEY

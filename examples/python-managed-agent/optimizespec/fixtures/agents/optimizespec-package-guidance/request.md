Create a eval workflow validation workflow for the package-guidance optimizer fixture.

The workflow should prove that a coding agent can generate eval artifacts for a GEPA optimization system, run direct eval, run compare, invoke GEPA optimization with a small metric-call budget, and preserve evidence. It should keep strict schemas for machine-consumed data and use semantic scoring for generated proposal/design/spec/task/apply prose.

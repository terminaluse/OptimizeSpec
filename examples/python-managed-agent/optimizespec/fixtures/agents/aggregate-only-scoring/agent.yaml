id: aggregate-only-scoring
name: aggregate-only-scoring
fixture_type: negative
runtime: Claude Managed Agents
description: Negative fixture where an eval design records only aggregate scores and omits case-level evidence.
source_files:
  runtime: src/optimizespec/runtime.py
existing_commands:
  direct_eval: optimizespec eval-demo
candidate_fields:
  - system_prompt
expected_failure: aggregate-only scoring
runtime_facts:
  - Aggregate scores are not enough to audit candidate behavior.
v1_constraints:
  - The workflow must require per-case score, judge, ASI, and rollout records.
expected_behavior:
  semantic_checks: {}
  live:
    requires_live: false
    required_env:
      - ANTHROPIC_API_KEY

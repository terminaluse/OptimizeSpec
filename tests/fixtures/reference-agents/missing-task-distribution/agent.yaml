id: missing-task-distribution
name: missing-task-distribution
fixture_type: negative
runtime: Claude Managed Agents
description: Negative fixture where examples exist but task distribution and edge cases are missing.
source_files:
  runtime: examples/py-claude-managed-agent/src/optimizespec/runtime.py
existing_commands:
  direct_eval: optimizespec eval-demo
candidate_fields:
  - system_prompt
expected_failure: missing task distribution
runtime_facts:
  - A single example is not enough to infer the real task distribution.
v1_constraints:
  - The workflow should ask for representative cases or mark distribution assumptions as unknown.
expected_behavior:
  semantic_checks: {}
  live:
    requires_live: false
    required_env:
      - ANTHROPIC_API_KEY

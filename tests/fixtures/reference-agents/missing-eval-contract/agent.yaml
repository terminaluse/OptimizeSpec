id: missing-eval-contract
name: missing-eval-contract
fixture_type: negative
runtime: Claude Managed Agents
description: Negative fixture where the request does not provide eval input/output examples.
source_files:
  runtime: examples/py-claude-managed-agent/src/optimizespec/runtime.py
existing_commands:
  direct_eval: optimizespec eval-demo
candidate_fields:
  - system_prompt
expected_failure: missing eval input/output
runtime_facts:
  - The runtime is valid, but the eval contract is absent.
v1_constraints:
  - The workflow must not invent expected outputs.
expected_behavior:
  semantic_checks: {}
  live:
    requires_live: false
    required_env:
      - ANTHROPIC_API_KEY

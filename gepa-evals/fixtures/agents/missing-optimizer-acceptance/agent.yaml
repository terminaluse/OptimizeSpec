id: missing-optimizer-acceptance
name: missing-optimizer-acceptance
fixture_type: negative
runtime: Claude Managed Agents
description: Negative fixture where the optimizer target is present but promotion and guardrail rules are missing.
source_files:
  runtime: src/agent_gepa/runtime.py
existing_commands:
  direct_eval: agent-gepa eval-demo
candidate_fields:
  - system_prompt
expected_failure: missing optimizer acceptance
runtime_facts:
  - A primary metric alone is not enough to decide whether to accept a GEPA candidate.
v1_constraints:
  - The workflow should require diagnostic metrics, guardrail metrics, promotion rules, regression tolerance, and evidence.
expected_behavior:
  semantic_checks: {}
  live:
    requires_live: false
    required_env:
      - ANTHROPIC_API_KEY

"""Template module.

For a maintained implementation, see `src/claude_gepa/self_improvement.py` in this repo.
Copy that module into a target repo and replace `TemplateEchoExecutor` with a Claude Managed
Agents executor that reuses the target repo's existing agent factories.
"""

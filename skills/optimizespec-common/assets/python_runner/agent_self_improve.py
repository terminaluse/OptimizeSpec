"""Template module.

For a maintained reference implementation, see
`examples/python-managed-agent/src/optimizespec/self_improvement.py` in this repo.
Copy that module into an agent project and replace `TemplateEchoExecutor` with a Claude Managed
Agents executor that reuses the project's existing agent factories.
"""
